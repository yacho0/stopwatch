import React, {Component} from 'react';


const Interface = (props) => {

    return (
    // <button onClick={props.stopWatch}>Stop</button>
    <button onClick={props.startWatch}>Start</button>
    )
}

export default Interface;